const arr = [1, 2, 3];
arr.push(2);

console.log(result);

// This is the first way of inserting arrays

// Time Complexity : O(1) . one element is added
// Space Complexity : O(1) .adding at the end
