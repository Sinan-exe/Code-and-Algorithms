function multiplyNumber(n) {
  return n * n;
}

console.log(multiplyNumber(2));

// This function perform single operations and no matter what the input is it will take the constant time complexity so it is O(1) complexity.

function addNumber(n) {
  return n + n;
}

console.log(addNumber(4));
