function printItems(n) {
  for (let i = 0; i < n; i++) {
    console.log(i);
  }
}

printItems(5);

// In this function the operation is completely dependent on the input value as the input value increases the number of opeartion increases sp it is called as Linear Time complexity O(N) as n increases the complexity increases.
